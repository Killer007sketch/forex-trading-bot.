name: Historical EURUSD backtest
on:
  push:
    paths:
      - '.github/workflows/historical-backtest.yml'
      - 'scripts/download_eurusd.py'
      - 'src/backtest.py'
      - 'src/trend_h4.py'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Download and validate EURUSD H1
        run: python scripts/download_eurusd.py --from-date '2016-01-01' --to-date '2026-09-17'
      - name: Baseline H1 comparison
        run: python src/backtest.py data/eurusd_h1.csv --initial 300 --spread-pips 1.5 --slippage-pips 0.3 --risk 0.005 | tee reports-baseline.txt
      - name: H4 real minimum lot 0.01 with 300 USD (feasibility check)
        run: python src/trend_h4.py data/eurusd_h1.csv --initial 300 --spread-pips 1.5 --slippage-pips 0.3 --risk 0.0025 --min-lot 0.01 | tee reports-trend-h4-300-minlot.txt
      - name: H4 hypothetical 0.0001 lot with 300 USD (NOT broker executable)
        run: python src/trend_h4.py data/eurusd_h1.csv --initial 300 --spread-pips 1.5 --slippage-pips 0.3 --risk 0.0025 --min-lot 0.0001 | tee reports-trend-h4-300-theoretical.txt
      - name: H4 real minimum lot 0.01 with 10000 USD (capital sensitivity, not recommendation)
        run: python src/trend_h4.py data/eurusd_h1.csv --initial 10000 --spread-pips 1.5 --slippage-pips 0.3 --risk 0.0025 --min-lot 0.01 | tee reports-trend-h4-10000-minlot.txt
      - name: Upload research outputs, not raw market data
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: eurusd-research-comparison
          path: |
            reports-*.txt
            reports/*.csv
          if-no-files-found: warn
